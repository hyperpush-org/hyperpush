---
phase: quick-7-get-isolated-peak-throughput-numbers-by-
task: in_progress
status: blocked_on_fly_bugs
last_updated: 2026-02-26T23:20:35.691Z
---

<current_state>
Running isolated benchmark on Fly.io. All machines cleaned up. Two script bugs found and partially fixed. Need one more rebuild+launch cycle with the final fix (`--yes` → `--force` on destroy commands) before the benchmark can run end-to-end.
</current_state>

<completed_work>

- Built server image remotely on Fly: `registry.fly.io/bench-mesh:deployment-01KJE16FS87JDVMS84FK5YG2DQ` (967MB, includes start-server-isolated.sh)
- Built loadgen image with fly CLI installed: multiple iterations fixing bugs
- Fixed bug 1: entrypoint conflict (Dockerfile CMD being passed as arg to --entrypoint)
- Fixed bug 2: machine ID extraction (regex matched image ID prefix before actual machine ID)
- Fixed bug 3: SERVER_READY detection via `fly logs` streams live-only, never sees already-emitted signals — removed log polling, go straight to HTTP poll
- Identified bug 4 (NOT YET FIXED IN IMAGE): `fly machine destroy --yes` fails in flyctl 0.4.15, flag is `--force` — machine stops but never destroys, causing "already_exists: unique machine name violation" on next language
- All stale machines cleaned up: machines 82d334c7357398 and 287d625a603448 destroyed
</completed_work>

<remaining_work>

- Fix `--yes` → `--force` in run-benchmarks-isolated.sh destroy commands (already done in local file, needs image rebuild)
- Also fix: machine destroy uses `--yes` in the end-of-language cleanup AND in the skip-on-failure cleanup
- Rebuild loadgen image and launch
- Wait ~15 min for benchmark to complete (4 languages × ~3.5 min each)
- Collect results from loadgen logs
- Update benchmarks/RESULTS.md with isolated numbers
- Update .planning/quick/6-write-an-article-about-the-benchmarks-in/ARTICLE.md with isolated numbers
- Complete quick task 7: commit artifacts, update STATE.md
</remaining_work>

<decisions_made>

- Server image tag: `registry.fly.io/bench-mesh:deployment-01KJE16FS87JDVMS84FK5YG2DQ` (not servers:latest — that image never existed)
- Loadgen runs `run-benchmarks-isolated.sh` as CMD (Dockerfile.loadgen CMD updated)
- Server launched with `--entrypoint /bin/bash` and script path as [command] — avoids execute-bit dependency
- Removed SERVER_READY log detection entirely — fly logs is live-streaming, can't see past events
- HTTP reachability poll is sufficient (server is ready when it answers HTTP)
- `--yes` is not a valid flag for flyctl 0.4.15 `machine destroy`; must use `--force`
</decisions_made>

<blockers>

- `--yes` → `--force` fix: already applied to local `run-benchmarks-isolated.sh` file, but the running loadgen image was built BEFORE this fix. Need one more rebuild.
- HTTP reachability: first clean run timed out after 120s. May need investigation — could be DNS propagation delay for `.internal` hostnames, or server startup taking longer than expected. Consider increasing HTTP poll timeout from 120s to 180s as a precaution.
</blockers>

<context>
This is quick task 7. The goal is isolated peak throughput numbers — each server gets its own dedicated `performance-2x` Fly machine, no co-location. The overall architecture is working: loadgen machine creates server machines per-language via fly CLI, benchmarks with hey, destroys them.

The main remaining issue is the `--yes` → `--force` destroy flag. Once that's fixed, the benchmark should run end-to-end. The local `run-benchmarks-isolated.sh` already has this fix applied.

After collecting numbers, update:
1. `benchmarks/RESULTS.md` — fill in the isolated tables (blank rows already exist)
2. `.planning/quick/6-write-an-article-about-the-benchmarks-in/ARTICLE.md` — add isolated numbers to the comparison section

The article file is at: /Users/sn0w/Documents/dev/snow/.planning/quick/6-write-an-article-about-the-benchmarks-in/ARTICLE.md
</context>

<next_action>
Start with: rebuild the loadgen image (the local run-benchmarks-isolated.sh already has --force fix) and launch:

```bash
FLY_TOKEN=$(fly auth token)
SERVER_IMAGE="registry.fly.io/bench-mesh:deployment-01KJE16FS87JDVMS84FK5YG2DQ"
fly machine run \
  --dockerfile benchmarks/fly/Dockerfile.loadgen \
  --app bench-mesh \
  --name bench-loadgen-isolated \
  --vm-size performance-2x \
  --region ord \
  --env "APP=bench-mesh" \
  --env "REGION=ord" \
  --env "SERVER_IMAGE=${SERVER_IMAGE}" \
  --env "FLY_API_TOKEN=${FLY_TOKEN}"
```

Then monitor: `fly logs --machine <new-machine-id> --app bench-mesh`

If HTTP reachability still times out after 120s, increase the poll loop from `seq 1 60` to `seq 1 90` (180s) in run-benchmarks-isolated.sh and rebuild again.
</next_action>
